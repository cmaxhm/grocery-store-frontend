/**
 * This enum uses the same values as the Tailwindcss library.
 * Check tailwind.config.js for more info.
 */
export enum RESPONSIVE_BREAKPOINTS {
  'md' = 600,
  'lg' = 900,
  'xl' = 1200,
  '2xl' = 1800
}
